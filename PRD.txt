서울아산병원 IDP는 React를 기반으로 **통합 포털**을 구축하는 것을 목표로 합니다. 이 포털은 BI, OLAP, ETL과 같은 표준 데이터 관리 기능에 대해서는 데이터스트림즈, 비아이매트릭스의 검증된 상용 솔루션을 연동하고, Text2SQL 등 병원 특화 AI 기능은 직접 개발하여 최첨단 AI 분석 환경을 제공하는 **하이브리드 전략**을 채택합니다.

---

## **🎯 React 기반 통합 포털 아키텍처**

### **기술 스택**

#### **Frontend (React Ecosystem)**
```json
{
  "core": {
    "react": "^18.2.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "routing": {
    "react-router-dom": "^6.20.0"
  },
  "state_management": {
    "zustand": "^4.4.7",
    "react-query": "^5.0.0"
  },
  "ui_framework": {
    "antd": "^5.12.0",
    "@ant-design/pro-components": "^2.6.0"
  },
  "visualization": {
    "recharts": "^2.10.0",
    "apache-echarts": "^5.4.0"
  },
  "api": {
    "axios": "^1.6.0"
  }
}
```

#### **Backend (FastAPI)**
```python
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25 # Custom AI modules may need this
asyncpg==0.29.0
redis==5.0.1
pydantic==2.5.3
# AI/LLM Stack from requirements.txt
langchain==0.1.5
langchain-community==0.0.17
...
```

---

## **프로젝트 구조**
```
C:\projects\datastreams\asan\
├── frontend/                    # React 포털 애플리케이션
│   ├── src/
│   │   ├── layouts/            # 레이아웃 (Sider, Header)
│   │   ├── pages/              # 7개 SFR 페이지
│   │   │   ├── Home/           # 홈 대시보드
│   │   │   ├── CommercialSolutionWrapper/ # 상용 솔루션 연동용 래퍼
│   │   │   ├── SFR006_AI/      # AI 분석 환경 (Custom)
│   │   │   └── SFR007_CDW/     # Text2SQL 등 연구 지원 (Custom)
│   │   ├── components/         # 공통 컴포넌트
│   │   ├── services/           # API 서비스 (AI 기능 위주)
│   │   ├── stores/             # Zustand 스토어
│   │   └── types/              # TypeScript 타입
│   └── package.json
│
├── backend/                     # FastAPI 백엔드 (AI 기능 위주)
│   ├── app/
│   │   ├── main.py
│   │   ├── api/
│   │   │   ├── v1/
│   │   │   │   ├── router.py
│   │   │   │   └── text2sql.py # 핵심 AI 기능
│   │   ├── services/           # 비즈니스 로직
│   │   │   └── text2sql_service.py
│   │   └── core/
│   ├── requirements.txt
│
└── docker/
    └── docker-compose.yml
```

---

## **Step-by-Step 구현 가이드**

### **Step 1: 프로젝트 초기화 (15분)**
(기존 내용과 동일)
```bash
# Frontend 초기화
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install antd @ant-design/pro-components react-router-dom zustand @tanstack/react-query axios ...

# Backend 초기화
cd ../backend
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

---

### **Step 2: Backend API 구축 (AI 기능 중심)**
Backend는 Text2SQL과 같은 RFP의 핵심 AI 요구사항(AAR) 구현에 집중합니다.

#### **`backend/app/main.py`** (예시)
```python
from fastapi import FastAPI
from app.api.v1.router import api_router

app = FastAPI(title="서울아산병원 IDP AI API")
app.include_router(api_router, prefix="/api/v1")

@app.get("/")
async def root():
    return {"message": "서울아산병원 IDP AI API"}
```

#### **`backend/app/api/v1/router.py`**
```python
from fastapi import APIRouter
from app.api.v1 import text2sql

api_router = APIRouter()
api_router.include_router(text2sql.router, prefix="/text2sql", tags=["Text2SQL"])
```

#### **`backend/app/api/v1/text2sql.py`** 🔥
(기존 Text2SQL API 구현 내용 유지)
```python
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel

router = APIRouter()

class Text2SQLRequest(BaseModel):
    question: str

@router.post("/generate")
async def generate_sql_from_text(request: Text2SQLRequest):
    # LangChain/LangGraph 기반 서비스 호출
    # ...
    return {"sql": "SELECT ...", "explanation": "..."}
```

---

### **Step 3: Frontend 포털 구조 구축**
Frontend는 전체 시스템의 관문(Gateway) 역할을 합니다. `MainLayout`을 통해 일관된 UI/UX를 제공하며, 각 메뉴는 자체 개발한 AI 기능 화면 또는 상용 솔루션 화면을 보여줍니다.

#### **`frontend/src/App.tsx`**
```tsx
import { Routes, Route } from 'react-router-dom'
import MainLayout from './layouts/MainLayout'
import Home from './pages/Home'
import CommercialSolutionWrapper from './pages/CommercialSolutionWrapper'
import SFR006AI from './pages/SFR006_AI'
import SFR007CDW from './pages/SFR007_CDW'

function App() {
  return (
    <Routes>
      <Route path="/" element={<MainLayout />}>
        <Route index element={<Home />} />
        {/* 상용 솔루션 연동 라우트 */}
        <Route path="datamart" element={<CommercialSolutionWrapper solution="TeraONE" />} />
        <Route path="bi" element={<CommercialSolutionWrapper solution="BIMatrixBI" />} />
        <Route path="olap" element={<CommercialSolutionWrapper solution="BIMatrixOLAP" />} />
        <Route path="etl" element={<CommercialSolutionWrapper solution="Terastream" />} />
        
        {/* 자체 개발 AI 기능 라우트 */}
        <Route path="ai" element={<SFR006AI />} />
        <Route path="cdw" element={<SFR007CDW />} />
      </Route>
    </Routes>
  )
}

export default App
```
#### **`frontend/src/layouts/MainLayout.tsx`**
(기존 레이아웃 구현 내용 유지, 메뉴 구성은 RFP와 일치)

---

### **Step 4: 상용 솔루션 연동 전략**
SFR-002, 003, 004, 005는 각 분야의 전문 상용 솔루션을 React 포털 내에 완벽하게 통합하여 제공합니다.

#### **`frontend/src/pages/CommercialSolutionWrapper.tsx`** (예시)
```tsx
import React from 'react';

interface Props {
  solution: 'TeraONE' | 'BIMatrixBI' | 'BIMatrixOLAP' | 'Terastream';
}

const solutionEndpoints = {
  TeraONE: 'https://datastreams.teraone.portal/login',
  BIMatrixBI: 'https://bimatrix.bi.portal/dashboard',
  BIMatrixOLAP: 'https://bimatrix.olap.portal/analysis',
  Terastream: 'https://datastreams.terastream.portal/monitoring'
};

const CommercialSolutionWrapper: React.FC<Props> = ({ solution }) => {
  const url = solutionEndpoints[solution];

  // SSO(Single Sign-On) 연동을 통해 사용자 인증 정보를 전달하고
  // <iframe>을 사용하여 포털 내에 해당 솔루션 화면을 Seamless하게 임베딩합니다.
  
  return (
    <div style={{ width: '100%', height: 'calc(100vh - 120px)' }}>
      <iframe
        src={url}
        title={solution}
        frameBorder="0"
        style={{ width: '100%', height: '100%' }}
      />
    </div>
  );
};

export default CommercialSolutionWrapper;
```

---

### **Step 5: SFR-007 Text2SQL React 구현** 🔥
(기존 Text2SQL UI 구현 내용 유지, 이는 핵심 자체 개발 기능)

---

## **Docker 배포 구성**
Docker Compose는 포털의 Frontend, Backend와 상용 솔루션이 의존하는 PostgreSQL, Redis 등 기반 서비스를 함께 관리합니다. 상용 솔루션 자체는 별도의 서버에 설치되는 것을 가정합니다.

(기존 docker-compose.yml 내용 유지, 단 backend 서비스는 AI 기능에 초점)
```yaml
version: '3.8'
services:
  postgres: ...
  redis: ...
  backend: # AI Backend
    ...
  frontend: # Portal Frontend
    ...
```

---

## **다음 단계**
이 아키텍처를 기반으로, **자체 개발이 필요한 AI 기능(SFR-006, SFR-007)의 상세 구현**과 **상용 솔루션 연동을 위한 SSO 및 임베딩 기술 검토**를 동시에 진행할 수 있습니다.

**Q: AI 기능 중 가장 먼저 구현할 프로토타입은 무엇일까요?**
- A) SFR-007: Text2SQL (가장 요구사항이 명확함)
- B) SFR-006: JupyterHub 연동 및 컨테이너 관리
- C) 기타 AI 기반 기능 (문서 요약, RAG 등)